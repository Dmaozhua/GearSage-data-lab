# Creator Inventory Notes

- Primary target: enumerate one Douyin creator homepage at a time.
- Primary output sheet: `creator_video_inventory`.
- `video_url` coverage is more important than perfect metadata completeness.
- Metadata fields like `publish_date` and `cover_text` are best-effort only.
- If the creator page loads but the works grid is blocked, keep the failure reason instead of inventing rows.
